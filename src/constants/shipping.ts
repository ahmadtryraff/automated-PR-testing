export enum ShippingStatus {
  PENDING = 'pending',
  IN_TRANSIT = 'inTransit',
  DELIVERED = 'delivered',
}

export enum ShippingMethod {
  ARAMEX = 'aramex',
  EXTERNAL = 'external',
  PENDING = 'pending',
} 